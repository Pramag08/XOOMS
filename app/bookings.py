"""
Deprecated: booking handlers have been moved to `app.customers` and `app.owner`.
This module is retained as a noop shim for backward compatibility.
Do not import `router` from here; use `/customers` or `/owner` routes instead.
"""
